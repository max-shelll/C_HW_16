# Работа с массивами в C

## 📋 Задания к лаб. 11

1. Заполнение массива с консоли
2. Преобразование массива
3. Печать массива
4. Найти сумму индексов элементов массива равных заданному числу А

## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции Func(x)
2.  Печать массива
3.  Вычисление суммы элементов массива
4.  Поиск в массиве значения равного a
5.  Подсчет количества пар эллементов, у которых меняется знак

## 📋 Задания к лаб. 16

1. Обработка элементов массива
2. Печать массива
3. Удаление элемента массива
4. Вставка `-999` в рандомное место массива
5. Выполнение домашнего задания (генерация массива на основе других массивов)

## 📁 Структура проекта

├── C_HW_16/

│   ├── C_HW_16.vcxproj

│   ├── C_HW_16.vcxproj.filters

│   ├── app.c

│   ├── array.h

│   └── arrays.c

├── .gitattributes

├── .gitignore

├── C_HW_16.sln

└── README.md


## 🔧 Реализованные функции

### 1. `double func(double x)`
- **Назначение**: Получение уникального вычисленного значения
- **Параметры**:
  - `x` — изменяемый параметр
- **Возвращает**: вычисленное уникальное значение

### 2. `double* full_elements(double* ptr_array, int n)`
- **Назначение**: Заполнение массива значениями
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `n` — размер массива
- **Возвращает**: указатель на заполненный массив

### 3. `int put_elements(double* ptr_array, int n)`
- **Назначение**: Вывод элементов массива
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `n` — размер массива
- **Возвращает**: количество выведенных элементов

### 4. `double* calc_elements(double* ptr_array, int n)`
- **Назначение**: Обработка элементов массива по индивидуальному варианту
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `n` — размер массива
- **Возвращает**: указатель на обработанный массив

### 5. `double sum_elements(double* ptr_array, int begin, int end)`
- **Назначение**: Вычисление суммы элементов массива в указанном диапазоне
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `begin` — начальный индекс (включительно)
  - `end` — конечный индекс (включительно)
- **Возвращает**: сумму элементов массива в диапазоне

### 6. `int find_element(double* ptr_array, int n, double element)`
- **Назначение**: Поиск элемента в массиве
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `n` — размер массива
  - `element` — искомое значение
- **Возвращает**: индекс элемента или -1, если элемент не найден

### 7. `int are_equal(double a, double b)`
- **Назначение**: Сравнение двух чисел
- **Параметры**:
  - `a` — первое число
  - `b` — второе число
- **Возвращает**: 1, если числа равны, иначе 0

### 8. `double* delete_k(double* ptr_array, int n, int k)`
- **Назначение**: Удаление элемента массива по индексу
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `n` — размер массива
  - `k` — индекс удаляемого элемента
- **Возвращает**: указатель на массив после удаления элемента

### 9. `double* insert(double* ptr_array, int n)`
- **Назначение**: Вставка значения -999 в случайную позицию массива
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `n` — размер массива
- **Возвращает**: указатель на массив после вставки

### 10. `double* make_new_array(double* A, int nA, double* B, int nB, double* C, int nC, int* nD)`
- **Назначение**: Создание нового массива на основе массивов A, B и C согласно заданному варианту
- **Параметры**:
  - `A` — указатель на массив A
  - `nA` — размер массива A
  - `B` — указатель на массив B
  - `nB` — размер массива B
  - `C` — указатель на массив C
  - `nC` — размер массива C
  - `nD` — указатель на размер нового массива
- **Возвращает**: указатель на новый созданный массив

### 11. `int countSignChanges(double* ptr_array, int n)`
- **Назначение**: Подсчёт количества изменений знака в массиве
- **Параметры**:
  - `ptr_array` — указатель на массив
  - `n` — размер массива
- **Возвращает**: количество изменений знака
